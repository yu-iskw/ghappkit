[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ghappkit_testing"]

[project]
name = "ghappkit-testing"
version = "0.1.0"
description = "Testing and simulation helpers for ghappkit."
requires-python = ">=3.10.0"
dependencies = []
