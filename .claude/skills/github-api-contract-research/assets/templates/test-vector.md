## Test Vector: <name>

- Operation:
- Surface (`rest`/`graphql`/`request`):
- Scenario (`success`/`non-retryable-failure`/`retryable-failure`):
- Preconditions:
- Input request:
- Expected result:
- Retry expectation:
- Source URL:
